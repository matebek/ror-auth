<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for?(:title) ? "#{yield(:title)} | " : "" %>RoRAuth</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex flex-col h-full antialiased text-slate-900 bg-gradient-to-r from-cyan-50 via-purple-50 to-pink-50">
    <%= render "partials/flash" %>
    <%= yield %>
  </body>
</html>
