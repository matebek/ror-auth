<div class="max-w-md m-auto p-4">
  <h1 class="text-3xl font-semibold"><%= yield :title %></h1>
  <div class="bg-white rounded-lg px-8 pt-6 pb-8 mt-6 shadow-xl">
    <%= yield %>
  </div>
  <div class="mt-6 text-center">
    <%= yield :footer %>
  </div>
</div>
